1. run `ng build test-lib`
2. run `npm run start:mfe`
3. run `npm run start:shell`
4. browse to http://localhost:4201  => this works fine
4. browse to http://localhost:4200  => Click on MFE link (you will get error in console)
